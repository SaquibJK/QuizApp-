function result(a, b, c,) {
   let prompt = `I want to learn ${a},i have ${b} experience and I am learning to ${c}, which programming language would you suggest for me`;
   return prompt;
}

export default result